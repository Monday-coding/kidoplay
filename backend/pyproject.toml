[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kidoplay-backend"
version = "0.1.0"
description = "Kidoplay - AI Learning Platform for Kids"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["backend*"]
